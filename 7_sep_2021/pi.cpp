#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    cout << acos(-1) << '\n';
    cout << acos(0) * 2 << '\n';
    cout << asin(1) * 2 << '\n';
    cout << asin(-1) * (-2) << '\n';
    cout << atan(1) * 4 << '\n';
    cout << acos(1 / 2) * 2 << '\n';

    return 0;
}